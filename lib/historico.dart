import 'package:flutter/material.dart';

class historico extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('historico'),
      ),
      body: Center(
        child: Text('Historico Page'),
      ),
    );
  }
}
