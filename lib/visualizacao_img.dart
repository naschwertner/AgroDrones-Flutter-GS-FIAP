import 'package:flutter/material.dart';

class visualizacao_img extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('visualizacao'),
      ),
      body: Center(
        child: Text('visualizacao'),
      ),
    );
  }
}
